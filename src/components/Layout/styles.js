import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  container: {
    background: "#fff",
    boxShadow: "-4px 7px 4px -7px rgba(0,0,0,0.55)",
    margin: "40px 0 80px 0"
  }
});

export default styles;
